Tokenizer: To calculate the token's how the models will calculate.

structure:

module1_guidelines/
|
|__pratice.py
|__requirementes.txt
|__student_assignment.md


program will take the input in from var text1 and the we will call the function:```python
show_tokens(text1, "bert-base-cased")
```